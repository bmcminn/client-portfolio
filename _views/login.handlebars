<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <meta name="robots" content="nofollow" />
  <link rel="shortcut icon" href="/favicon.png">
  <link rel="stylesheet" href="{{url /resources/login.css }}">
</head>
<body>

  <form action="{{url /login }}" method="POST">
    <h1>Login</h1>
    {{#if formError }}
    <ul class="error">
      {{#each formError }}
        <li>{{ . }}</li>
      {{/each}}
    </ul>
    {{/if}}
    <label for="username">
      Username
      <input name="username" id="username" type="text" value="{{ loginInfo.username }}">
    </label>


    <label for="password">
      Password
      <input name="password" id="password" type="password" value="">
    </label>

    <button type="submit">Submit</button>
  </form>

</body>
</html>
